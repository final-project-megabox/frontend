export interface RankMovie {
  movie_id: number,
  age: string | number,
  title: string,
  booking_rate: string,
  release_date: string,
  img_url: string
}

export interface RankStarContent {
  id: number,
  starContent: string
}