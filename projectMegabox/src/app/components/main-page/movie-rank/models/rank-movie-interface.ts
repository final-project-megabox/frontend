export interface RankMovie {
  movie_id: number,
  age: number,
  title: string,
  booking_rate: string,
  release_date: string,
  img_url: string,
  selected: boolean
}

export interface RankStarContent {
  id: number,
  starContent: string
}